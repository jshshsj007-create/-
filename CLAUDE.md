# قواعد العمل في هذا المستودع

تطبيق **فريق فيض السعودي**: برامج وأيام ومشتركون ومال، ورابطان عامّان يفتحهما
أولياء الأمور (التسجيل وسؤال اليوم). يشتغل على الناس فعلًا — تسجيلاتٌ ومبالغُ
وأجوبةٌ تصل كل يوم — فالقواعد تحت ليست ترتيبًا للبيت، بل مكتوبةٌ من أعطابٍ وقعت.

## أين يعيش كل شيء

```
src/App.tsx             التطبيق (مكوّن واحد كبير + دوال الحساب المصدَّرة للاختبار)
src/SignupPage.jsx      صفحة وليّ الأمر — خارج التطبيق، بلا تسجيل دخول
src/QuestionPage.jsx    سؤال اليوم — كذلك
src/*.js                منطقٌ خالصٌ يُختبر بلا متصفّح (signup, club, money, watch, adad …)
netlify/functions/      الخادم: state.mjs وحده، وكل العمليات تمرّ به
netlify/lib/            النسخ الاحتياطي وكلمات المرور
tests/run.sh            كل الاختبارات
```

## النشر — اقرأ هذا قبل أن تدفع

**الفرع `claude/programs-revenue-management-mqi2d1` هو فرع الإنتاج.** كل دفعةٍ إليه
تنشر الموقع الحيّ فورًا. لا يوجد فرعٌ وسيطٌ ولا مراجعة.

**وكل نشرةٍ تُكلّف صاحب التطبيق ١٥ رصيدًا من Netlify**، وحصّته ٣٠٠ في الشهر — أي
**عشرون نشرة**. وقد استُهلكت حصّةُ شهرٍ كامل في دورةٍ واحدة (٩٠ نشرة) لأن كل إصلاحٍ
نُشر وحده، فتوقّف النشر أسبوعين ودفع صاحبُه ليستأنف.

فالقاعدة: **نشرةٌ واحدة في الجلسة**، تُجمع فيها كل التعديلات، إلا أن يطلب صاحبُ
التطبيق النشر في حينه. والاستعمال الحقيقي — ثمانيةٌ وعشرون ألف طلبٍ وكلُّ التحميل —
كلّف ثلاثين رصيدًا، أي ثمنَ نشرتين. فالنشر وحده هو الذي يُحسب.

## الفحص قبل النشر

**افتح التطبيق بمتصفّحٍ حقيقي قبل كل نشرة.** الاختبارات تستبدل React ببدائل لا
ترسم شيئًا، فمرّت ٦٢٧ اختبارًا يومًا والتطبيق لا يفتح أصلًا (شاشةٌ بيضاء من خطأ
ترتيب الخطّافات). والبدائل مقصودةٌ — تخلّي دوال الحساب تُختبر في Node بلا DOM —
لكنها لا تشهد للرسم بشيء.

```bash
npm test            # ./tests/run.sh — يحتاج Node فقط
npm run build       # ينسخ عامل pdf.js إلى public/ ثم يبني
```

وللفحص بمتصفّح: ابنِ، ثم شغّل خادمًا محليًّا يقدّم `dist/` ويشغّل `state.mjs` بمخزنٍ
في الذاكرة، ثم افتح الصفحات بـ Playwright (المتصفّح في `/opt/pw-browsers/`). ومن
غيّر شيئًا في الملفات المرفوعة فليرفع ملفًّا حقيقيًّا ويقرأه، لا يكتفِ بالنظر.

## ما لا يُكتب في الشيفرة أبدًا

المستودع **عام**. فالآيبان ورقم STC وأي مفتاح: لا في الشيفرة ولا في التعليقات ولا
في رسائل الالتزام. و`DRIVE_HOOK_URL` و`DRIVE_HOOK_SECRET` يضعهما صاحبُ التطبيق
بنفسه في إعدادات Netlify، ولا تُطلب منه في المحادثة ولا تُطبع.

ولا يُذكر اسم النموذج في أي أثرٍ يدخل المستودع.

## البيانات — ما يجب أن تعرفه قبل أن تلمس الخادم

كلُّ شيء في مخزنٍ واحد: `getStore({ name: 'faid-team', consistency: 'strong' })` —
البيانات والدفاتر الثلاثة (`led:sub:` التسجيلات، `led:ans:` الأجوبة، `led:mal:`
المال) واللقطات والصور. وهو **مشتركٌ بين كل نشرات الموقع**.

- الكتابة بنسخةٍ متفائلة: `baseRev` ثم دمجٌ ثلاثيّ، والحمولةُ ورقمُها لا يفترقان.
- `guard()` في الخادم يمنع المحوَ بلا سجلّ حذف، ويحمي الروابط والسؤال والمؤقّت من
  جهازٍ قديمٍ يظنّها غير موجودة — **غيابُ الحقل ليس إذنًا بحذفه**.
- الدفاتر لا تُمحى، ومنها يُسترجع من ضاع. وحدها نسخةُ درايف تنجو لو مات المخزن.

## اللغة

كل ما يراه صاحب التطبيق ووليُّ الأمر عربي. والتعليقات ورسائل الالتزام عربيةٌ كذلك،
تقول **لماذا** كُتب السطر لا ماذا يفعل. وتمييز العدد يمرّ بـ `say()` في `src/adad.js`
— «١٩ ساعة» لا «١٩ ساعات».
